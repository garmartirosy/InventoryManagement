@page
@model InventoryManagement.Pages.Suppliers.IndexModel

<h1>Suppliers</h1>

<form method="post">
    <input asp-for="Supplier.Id" type="hidden" />

    <div>
        <label asp-for="Supplier.Name"></label>
        <input asp-for="Supplier.Name" />
        <span asp-validation-for="Supplier.Name"></span>
    </div>

    <button type="submit">Save</button>
    <a asp-page="./Index">Clear</a>
</form>

<table>
    <thead>
        <tr>
            <th>Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var supplier in Model.Suppliers)
        {
            <tr>
                <td>@supplier.Name</td>
                <td>
                    <a asp-page="./Index" asp-route-id="@supplier.Id">Edit</a>
                    <form method="post" asp-page-handler="Delete" asp-route-id="@supplier.Id">
                        <button type="submit">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
